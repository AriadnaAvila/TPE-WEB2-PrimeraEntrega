<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Tienda</title>
    <!-- Favicon -->
    <link rel="icon" href="assets/images/favicon.ico" type="image/x-icon">
    <!-- Incluye Tailwind CSS -->
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
<header id="header" class="bg-gray-800 text-white p-5 fixed top-0 left-0 right-0 z-50 transition-all duration-300">
    <nav class="container mx-auto flex justify-between items-center">
        <div class="flex items-center">
            <img src="assets/images/tienda-logo.jpg" alt="Logo de Mi Tienda" class="h-10 mr-4">
            <a href="home" class="text-xl font-semibold">Mi Tienda</a>
        </div>
        <div class="flex space-x-4">
            <a href="categories" class="text-white">Categorías</a>
            <a href="products" class="text-white">Productos</a>
        </div>
        <div class="flex space-x-4">
            <?php if (isset($_SESSION['user'])): ?>
                <span class="text-sm">Bienvenido, <?= htmlspecialchars($_SESSION['user']) ?></span>
                <a href="logout" class="text-red-500">Cerrar Sesión</a>
            <?php else: ?>
                <a href="home" class="text-white">Iniciar Sesión</a>
            <?php endif; ?>
        </div>
    </nav>
</header>

<main class="container mx-auto mt-20">
    <!-- Aquí va el contenido de las categorías, productos, etc. -->
</main>



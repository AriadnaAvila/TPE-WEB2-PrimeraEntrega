<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Tienda</title>
    <link rel="icon" href="assets/images/favicon.ico" type="image/x-icon">
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <script>
        tailwind.config = {
            theme: {
                extend: {
                    fontFamily: {
                        sans: ['Inter', 'sans-serif'],
                    },
                }
            }
        }
    </script>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            opacity: 0;
            transition: opacity 0.5s ease-in-out;
        }

        body.loaded {
            opacity: 1;
        }
    </style>
</head>
<body class="bg-white text-gray-900">
    <header class="bg-white shadow-sm">
        <nav class="container mx-auto px-4 py-4 flex items-center justify-between">
            <div class="flex items-center space-x-10">
                <a href="home" class="flex items-center">
                    <span class="ml-3 text-xl font-bold tracking-tight">Outlet</span>
                </a>
                <div class="hidden md:flex space-x-6">
                    <a href="categories" class="text-gray-700 hover:text-black transition-colors font-medium">Categorías</a>
                    <a href="products" class="text-gray-700 hover:text-black transition-colors font-medium">Productos</a>
                </div>
            </div>
            <div class="flex items-center space-x-4">
                <?php if (isset($_SESSION['user'])): ?>
                    <span class="text-sm text-gray-600">Hola, <?= htmlspecialchars($_SESSION['user']) ?></span>
                    <a href="logout" class="text-sm font-medium text-gray-700 hover:text-black transition-colors">Salir</a>
                <?php else: ?>
                    <a href="home" class="text-sm font-medium text-gray-700 hover:text-black transition-colors">Iniciar Sesión</a>
                <?php endif; ?>
            </div>
        </nav>
    </header>

    <main class="container mx-auto px-4 py-8">
        <!-- Aquí va el contenido de las categorías, productos, etc. -->
    </main>

    <script>
        window.onload = function() {
            document.body.classList.add('loaded');
        };
    </script>
</body>
</html>

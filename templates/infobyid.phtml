<?php require './templates/header.phtml'; ?>

<div class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-12">
        <h1 class="text-4xl md:text-5xl font-bold text-gray-900 mb-8">Detalles del Producto</h1>

        <?php if ($information): ?>
            <div class="bg-white shadow-lg rounded-lg overflow-hidden">
                <div class="md:flex">
                    <div class="md:flex-shrink-0">
                        <img class="h-96 w-full object-cover md:w-96" src="https://via.placeholder.com/400x400" alt="Imagen del producto">
                    </div>
                    <div class="p-8">
                        <div class="uppercase tracking-wide text-sm text-black font-semibold"><?= htmlspecialchars($information->nombre_categoria) ?></div>
                        <h2 class="mt-2 text-3xl leading-8 font-bold text-gray-900"><?= htmlspecialchars($information->tipo) ?></h2>
                        <p class="mt-4 text-2xl font-bold text-green-600">$<?= htmlspecialchars($information->precio) ?> ARS</p>

                        <div class="mt-6 border-t border-gray-200 pt-6">
                            <h3 class="text-lg font-semibold text-gray-900 mb-2">Detalles del Producto</h3>
                            <div class="grid grid-cols-2 gap-4">
                                <div>
                                    <p class="text-sm font-medium text-gray-500">Talle</p>
                                    <p class="mt-1 text-sm text-gray-900"><?= htmlspecialchars($information->talle) ?></p>
                                </div>
                                <div>
                                    <p class="text-sm font-medium text-gray-500">Color</p>
                                    <p class="mt-1 text-sm text-gray-900"><?= htmlspecialchars($information->color) ?></p>
                                </div>
                                <div>
                                    <p class="text-sm font-medium text-gray-500">Stock Disponible</p>
                                    <p class="mt-1 text-sm text-gray-900"><?= htmlspecialchars($information->stock) ?></p>
                                </div>
                            </div>
                        </div>
                        <?php if (isset($_SESSION['user'])): ?>
                            <div class="mt-8 flex space-x-4">
                                <a href="editProduct/<?= $information->id_producto ?>" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-gray-800 hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500">
                                    Editar Producto
                                </a>
                                <a href="deleteProduct/<?= $information->id_producto ?>" class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-gray-700 bg-gray-100 hover:bg-gray-200 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500">
                                    Eliminar Producto
                                </a>
                            </div>
                        <?php endif; ?>
                    </div>
                </div>
            </div>
        <?php else: ?>
            <div class="bg-red-100 border-l-4 border-red-500 text-red-700 p-4 mb-8" role="alert">
                <p class="font-bold">Error</p>
                <p>No se encontró información para este producto.</p>
            </div>
        <?php endif; ?>
    </div>
</div>

<?php require './templates/footer.phtml'; ?>